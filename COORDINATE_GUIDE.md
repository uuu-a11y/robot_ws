# 机械臂与导航坐标配置指南

## 一、机械臂坐标点（单位：mm）

### 1.1 关键位置坐标

| 位置名称 | X (前后) | Y (左右) | Z (上下) | 快捷键 | 说明 |
|---------|---------|---------|---------|-------|------|
| **安全/复位位置** | 150 | 0 | 200 | R | 机械臂归零位置 |
| **摄像头位置** | 90 | 120 | 100 | G | 手部相机能看到物块的位置 |
| **台面摄像头位置** | 110 | -170 | 100 | T | 看台面AR码的位置 |
| **台面放置位置** | 80 | -190 | 30 | U | 物块放到台面的位置 |

### 1.2 储物槽位置

| 位置名称 | X | Y | Z | 快捷键 | 说明 |
|---------|---|---|---|-------|------|
| **前储物槽** | 110 | 120 | 40 | B | 小车前方储物位置 |
| **后储物槽** | 110 | 180 | 40 | N | 小车后方储物位置 |

### 1.3 机械臂坐标范围

| 坐标轴 | 最小值 | 最大值 | 说明 |
|-------|-------|-------|------|
| X | 0 | 280 | 前后方向 |
| Y | -278 | 278 | 左右方向 |
| Z | 0 | 130 | 上下方向 |

---

## 二、导航坐标点（单位：m）

### 2.1 预设工位

| 工位名称 | X (m) | Y (m) | AR码ID | 说明 |
|---------|-------|-------|--------|------|
| **起点** | 0.0 | 0.0 | - | 小车初始位置 |
| **5号工位** | 0.60 | 1.20 | 1 | 上料点/加工中心 |
| **1号工位** | 2.00 | 2.20 | 1 | 取料点/加工中心 |

### 2.2 AR码配置

| AR码ID | 用途 | 位置 |
|--------|------|------|
| **1** | 所有台面定位 | 地面上的AR标记 |

### 2.3 对准距离

| 参数 | 值 | 说明 |
|-----|-----|------|
| goal_dist | 0.25m | 底盘对准AR码时的目标距离 |

---

## 三、快捷操作流程

### 3.1 从储物槽抓取 → 放置到台面

```
1. 按 G  → 移动到摄像头位置 (90, 120, 100)
2. 空格  → 开启气泵抓取
3. 按 U  → 移动到台面放置位置 (80, -190, 30)
4. 空格  → 关闭气泵放置
5. 按 R  → 归位 (150, 0, 200)
```

### 3.2 从台面抓取 → 放置到储物槽

```
1. 按 T  → 移动到台面摄像头位置 (110, -170, 100)
2. 空格  → 开启气泵抓取
3. 按 B  → 移动到前储物槽 (110, 120, 40)
4. 或按 N  → 移动到后储物槽 (110, 180, 40)
5. 空格  → 关闭气泵放置
6. 按 R  → 归位
```

---

## 四、文件位置

### 4.1 机械臂控制脚本
```
~/robot_ws/src/oryxbot_description/src/arm_keyboard.py   # 键盘控制
~/robot_ws/src/oryxbot_description/src/arm_test.py        # 菜单控制
```

### 4.2 导航控制脚本
```
~/robot_ws/src/oryxbot_description/src/nav_keyboard.py    # 键盘控制
~/robot_ws/src/oryxbot_description/src/nav_test.py        # 菜单控制
```

### 4.3 启动脚本
```
~/robot_ws/start_arm_test.sh     # 机械臂测试启动
~/robot_ws/start_nav_test.sh    # 导航测试启动
~/robot_ws/start_nav_slam.sh    # SLAM导航启动
~/robot_ws/start_robot.sh      # 完整任务启动
```

### 4.4 地图文件
```
~/robot_ws/src/oryxbot_navigation/maps/my_map.pgm    # 建好的地图
~/robot_ws/src/oryxbot_navigation/maps/my_map.yaml
```

---

## 五、使用说明

### 5.1 机械臂调试
```bash
./start_arm_test.sh
# 选择 1: 键盘控制
# 选择 2: 菜单控制
```

### 5.2 导航调试
```bash
./start_nav_slam.sh
# 选择 1: 使用已建地图导航
# 选择 2: SLAM实时建图
```

### 5.3 完整任务
```bash
./start_robot.sh
```

---

## 六、重要提示

1. **机械臂坐标单位是毫米(mm)**，导航坐标单位是米(m)
2. **所有台面的AR码ID都是1**
3. **对准距离设置为0.25m**
4. 机械臂Z轴最大值约130mm，不要超出范围
5. 导航前需要先用"2D Pose Estimate"设置机器人初始位置

---

*最后更新：2026-04-04*
